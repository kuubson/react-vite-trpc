{
  "name": "react-vite-trpc",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.18.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "dependencies": {
    "turbo": "^1.10.3"
  },
  "devDependencies": {
    "eslint-config-custom": "*"
  }
}
